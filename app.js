const labels={leadership:'Leadership',service:'Service',drill:'Drill & Color Guard',adventure:'Adventure',academics:'Academics',events:'Events'};let photos=[],visible=[],current=0;
async function load(){try{const r=await fetch('gallery-data.json',{cache:'no-store'});photos=await r.json();visible=[...photos];photoCount.textContent=String(photos.length).padStart(2,'0');render()}catch(e){loadError.hidden=false}}
function esc(s=''){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function render(f='all'){galleryGrid.innerHTML='';visible=f==='all'?[...photos]:photos.filter(p=>p.category===f);visible.forEach((p,i)=>{let c=document.createElement('article');c.className='card '+(p.layout||'normal');c.innerHTML=`<img src="${p.image}" alt="${esc(p.title)}"><i>${String(i+1).padStart(2,'0')}</i><div><small>${labels[p.category]||p.category}</small><h3>${esc(p.title)}</h3><p>${esc(p.caption||'')}</p></div>`;c.onclick=()=>openLB(i);galleryGrid.appendChild(c)})}
function openLB(i){if(!visible.length)return;current=i;update();lightbox.classList.add('open');document.body.classList.add('no-scroll')}function update(){const p=visible[current];lightboxImage.src=p.image;lbCategory.textContent=labels[p.category]||p.category;lbTitle.textContent=p.title;lbCaption.textContent=p.caption||'';lbCount.textContent=`${String(current+1).padStart(2,'0')} / ${String(visible.length).padStart(2,'0')}`;bigNum.textContent=String(current+1).padStart(2,'0')}function closeLB(){lightbox.classList.remove('open');document.body.classList.remove('no-scroll')}function next(){current=(current+1)%visible.length;update()}function prev(){current=(current-1+visible.length)%visible.length;update()}
document.querySelectorAll('.filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)});randomBtn.onclick=()=>{visible=[...photos];openLB(Math.floor(Math.random()*visible.length))};closeBtn.onclick=closeLB;nextBtn.onclick=next;prevBtn.onclick=prev;document.onkeydown=e=>{if(!lightbox.classList.contains('open'))return;if(e.key==='Escape')closeLB();if(e.key==='ArrowRight')next();if(e.key==='ArrowLeft')prev()};load();
