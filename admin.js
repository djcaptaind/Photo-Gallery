let items=[],upload='';const STORE='callawayGalleryDraftV3',labels={leadership:'Leadership',service:'Service',drill:'Drill & Color Guard',adventure:'Adventure',academics:'Academics',events:'Events'};
async function init(){let s=localStorage.getItem(STORE);if(s)try{items=JSON.parse(s)}catch(e){};if(!items.length)try{items=await(await fetch('gallery-data.json',{cache:'no-store'})).json()}catch(e){};render()}
function save(){localStorage.setItem(STORE,JSON.stringify(items))}function toast(m){toastEl.textContent=m;toastEl.classList.add('show');setTimeout(()=>toastEl.classList.remove('show'),2000)}const toastEl=document.getElementById('toast');
document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');let l=b.dataset.tab==='local';localTab.hidden=!l;urlTab.hidden=l});fileInput.onchange=e=>{let f=e.target.files[0];if(!f)return;filename.textContent=f.name;let r=new FileReader();r.onload=()=>{upload=r.result;toast('Photo ready')};r.readAsDataURL(f)};
addBtn.onclick=()=>{let local=document.querySelector('.tab.active').dataset.tab==='local',image=local?upload:urlInput.value.trim(),title=titleInput.value.trim();if(!image)return toast('Choose a photo or enter a URL');if(!title)return toast('Enter a title');items.push({image,title,caption:captionInput.value.trim(),category:categoryInput.value,layout:layoutInput.value});save();render();upload='';fileInput.value='';filename.textContent='No photo selected';urlInput.value='';titleInput.value='';captionInput.value='';toast('Photo added')};
function render(){count.textContent=items.length;cards.innerHTML='';if(!items.length){cards.innerHTML='<div class="empty">No photos yet. Add one from the panel.</div>';return}items.forEach((p,i)=>{let c=document.createElement('article');c.className='photo';c.innerHTML=`<img src="${p.image}"><div><small>${labels[p.category]||p.category} • ${p.layout||'normal'}</small><h3>${p.title}</h3><p>${p.caption||''}</p><section class="buttons"><button data-a="up">↑</button><button data-a="down">↓</button><button data-a="edit">EDIT</button><button data-a="delete">DELETE</button></section></div>`;c.querySelectorAll('button').forEach(b=>b.onclick=()=>act(b.dataset.a,i));cards.appendChild(c)})}
function act(a,i){if(a==='up'&&i>0)[items[i-1],items[i]]=[items[i],items[i-1]];if(a==='down'&&i<items.length-1)[items[i+1],items[i]]=[items[i],items[i+1]];if(a==='delete'){if(!confirm('Delete this photo?'))return;items.splice(i,1)}if(a==='edit'){let t=prompt('Photo title:',items[i].title);if(t===null)return;let c=prompt('Caption:',items[i].caption||'');if(c===null)return;items[i].title=t.trim()||items[i].title;items[i].caption=c.trim()}save();render()}
exportBtn.onclick=()=>{let blob=new Blob([JSON.stringify(items,null,2)],{type:'application/json'}),u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download='gallery-data.json';a.click();URL.revokeObjectURL(u);toast('gallery-data.json exported')};importBtn.onclick=()=>importInput.click();importInput.onchange=e=>{let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>{try{let x=JSON.parse(r.result);if(!Array.isArray(x))throw 0;items=x;save();render();toast('Gallery imported')}catch(e){toast('Invalid gallery JSON')}};r.readAsText(f)};init();
